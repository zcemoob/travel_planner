language: python
python:
  - "3.7"
  - "3.8"
# command to install dependencies
install:
  - pip install requests pyyaml pytest pytest-cov matplotlib numpy

# command to run tests
script:
  - pytest --cov=./

  #trying to setup travis